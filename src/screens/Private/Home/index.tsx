import { Background, Header, Typography } from '../../../components';

export function Home() {
  return (
    <Background>
      <Header variant='logout' />

      <Typography text='Home' textAlign='center' />
    </Background>
  );
}
